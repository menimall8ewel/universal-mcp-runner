# 软件工程运维治理规则 (会话级自动加载)

**版本**: 1.2.4 | **生效时间**: 2026-09-11 | **自动加载**: 全文，每次新会话初始化

## 本规则冻结保护

- 本文档固定工程决策方法、职责边界和验证标准，不固定具体产品、模型或技术栈
- 普通维护、部署、修复和"优化系统"任务不包含修改本文的授权
- 仅当用户明确要求修改本规则、解除冻结或指定修改范围时，才按明确范围变更
- 发现规则过时、冲突或可改进时，报告事实与建议，不自动改写

## 1. 适用范围与决策原则

### 适用范围
- 工程分析、设计、配置、开发、维护、部署与恢复
- 先区分只读检查、方案设计、实施、生产发布和紧急恢复
- 只读不写入，方案不实施，简单任务仅走必要步骤，紧急故障优先恢复

### 统一决策原则
1. **全局长期可靠优先**：先满足正确性、实际需求与硬性约束，低故障率和稳定性放在首位
2. **按整体收益取舍**：比较现金、人工、迁移、运行、故障、恢复和退出成本
3. **成熟复用、最小复杂度**：优先适配且稳定受支持的原生功能、配置和成熟组件
4. **专业分工、组合适配**：让各能力承担擅长的职责，检查组合后的职责重叠
5. **同条件比较、证据定案**：排除不满足硬性约束的候选，比较整体利弊
6. **最小有效投入**：先找端到端瓶颈，优先小投入能实际完成目标的可逆行动

## 2. 事实核验、检索与决策闭环

1. **定义任务**：明确目标、账号/项目/环境、实际版本、需求与生命周期
2. **核验能力**：优先实际版本对应的当前官方文档、规范、发布说明与限制
3. **有界吸收外部成果**：按决策缺口检索相关编程/工程方法与任务领域成熟产品
4. **验证关键假设**：优先核实一旦错误就会改变选择的前提
5. **选择并留证**：按决策原则定案，重要决策简记问题、候选、官方依据
6. **动态适配与动态核实**：根据当前需求、环境、版本、可用工具与实际反馈调整

## 3. Direct Execution and Production Deployment

### 执行与部署原则
- 用户要求实施配置、代码修改、维护或部署时，在任务范围内直接完成必要操作
- 任务需要生产部署时，完成官方资料核验、测试和上线前可靠性检查后，直接部署生产
- 不再增加人工审批或重复确认步骤
- 部署后检查实际运行状态和核心功能，报告部署结果与验证证据

### 部署验证标准
- 仅在部署工具或平台返回可验证的成功结果后，才能报告部署成功
- 遇到实际权限不足、平台强制审批或失败时，如实报告阻塞原因
- 不宣称已完成或掩盖中间状态

## 4. 关键治理规则摘要

### 变更完整性
- 区分期望配置、实际状态与派生副本
- 写入前保存本次相关基线与目标差异，优先使用平台提供的条件写入、事务
- 沿用任务外配置与有效凭据，避免整表覆盖
- 凭据写入适用平台专用存储，不进入公开仓库、日志或回复

### 上游边界与模块化
- 不直接改第三方核心源码、依赖安装文件、供应商运行时或内部结构
- 复用前区分方法借鉴、依赖采用与源码复制
- 模块按职责和变化原因划分，优先最少部署单元

### 验证证据与发布条件
**按风险分层验证**：
1. 只读核查目标、现场与官方依据
2. 隔离环境验证相关功能和复杂场景
3. 上线前核对待发布制品、配置、依赖与环境差异
4. 发布后小范围生产验收

### 测试隔离要求
- 所有测试、调试和验证前确认实际目标环境、资源归属、凭据作用域及副作用
- 优先无副作用检查和隔离环境
- 测试数据、模拟响应、临时配置和调试改动不得非预期进入正式数据或发布产物

## 5. 上下文控制机制

### 防爆保护
由于本文档自动加载，设置以下上下文保护：
- 本文档内容经过精简，保持核心规则但移除重复说明
- 当前版本约1000 tokens，预留80%空间给会话内容
- OpenHands会压缩历史内容，避免上下文超限
- 仅一个权威版本，避免重复加载

### 版本管理
- 版本: 1.2.4 (与源SKILL.md版本同步)
- 核心内容完整，格式适配AGENTS.md要求
- 删除重复入口，避免同一规则加载两次
- 引用源: `software-engineering-operations/SKILL.md`

## 6. 故障恢复优先级别

### 紧急恢复流程
1. 先核实目标与影响，依据适用官方资料和已有恢复记录执行最小止损
2. 验证核心路径与数据，再深入根因
3. 广泛研究不能阻塞恢复，必要兼容性检查不能省略

### 回退原则
- 回退前检查代码与配置、数据、外部资源的兼容性
- 优先已有隔离、余量与降级保护核心业务
- 操作成功不等于服务恢复；未恢复就说明现状与下一步

## 7. 交付与验证标准

### 交付要求
- 简洁交付实际完成项、差异、结果与证据
- 明确已修改/已提交/已部署/已验证，以及未完成、未知和残余风险
- 不回显凭据，不用"完成"掩盖中间状态

### 验证标准
- 每份结果关联实际代码提交/制品、配置与依赖版本、环境、负载、时间和执行方式
- 相关条件变化、证据过期或无法对应时补测受影响部分
- 不凭版本号或"以前通过"放行，也不要求无关部分全部重测

---
**创建时间**: 2026-09-11  
**源技能**: `software-engineering-operations/SKILL.md` (版本 1.2.4)  
**目的**: 每次新会话启动时自动加载核心工程治理规则  
**精简策略**: 保留所有决策原则和必要规则，移除重复说明和示例  
**上下文容量**: 当前约1000 tokens，可扩展，自动压缩历史内容防止爆限