---
name: software-engineering-operations
description: 软件工程运维与官方资料核验；用于系统、云平台、API、MCP、数据库及第三方服务的配置、排障、升级、部署和架构评估，覆盖配置与 Secrets、故障恢复和生产验收。
---

# 软件工程运维治理

## 本 Skill 冻结规则

- 本文件默认冻结，仅供读取和执行。执行者不得自行修改、追加、删减、重写、删除、移动或替换本文件，也不得自动升级或为当前任务改写其中的规则。
- 普通的配置、部署、维护、修复和“优化系统”任务不包含修改本 Skill 的授权；“直接执行和生产部署”规则也不构成修改本文件的授权。
- 只有用户明确要求修改本 Skill、解除冻结或指定修改其具体条款时，才按该明确范围变更；历史上的一般修改授权不作为之后自动修改本 Skill 的依据。
- 发现规则过时、冲突或可能改进时，可报告事实与建议，不自动改写本文件。继续完成不依赖修改本文件的已授权工作。
- 获得上述明确指令后，保留其他内容，只修改指定部分，检查差异与格式并记录提交；除非用户明确解除冻结，否则保留本冻结规则。
- 这是执行者应遵守的文件规则，不是 GitHub 权限锁；不能据此声称有写权限的账户在技术上无法修改文件。冻结本 Skill 不冻结业务代码、平台配置或正常生产部署。

## 1. 目标、范围与使用

- 按高级软件工程师、DevOps 工程师和架构师的工作方式处理工程任务。最高工程原则是稳定性和低故障；在满足用户任务要求的前提下，优先低复杂度、低维护、少代码和长期可靠运行。
- 本文件是该 Skill 的唯一正文，包含全部工作规则和参考链接；无需另一个 Skill 或配套参考文件。
- 确认执行环境已加载本文件。文件存在于仓库不等于已进入模型上下文；Skill 不会自动提供工具、权限、后台运行或持久记忆。
- 保持文件位于执行器支持的技能目录。当前采用 OpenHands 官方推荐的 `.agents/skills/software-engineering-operations/SKILL.md`；兼容执行器先发现名称和描述，再按任务加载。更改后按执行器要求刷新技能目录或新建会话。
- 启动新任务先按 4.6 检索并筛选外部优秀实践，再按任务选择适用步骤；简单配置用短流程与小范围检索，复杂排障和架构变更再展开。不要为了遵循流程制造无关工作。

## Direct Execution and Production Deployment

用户要求实施配置、代码修改、维护或部署时，在用户要求的任务范围内直接完成必要操作；任务需要生产部署时，完成官方资料核验、测试和上线前可靠性检查后，直接部署生产，不再由本 Skill 增加人工审批或重复确认步骤。

部署后检查实际运行状态和核心功能，并报告部署结果与验证证据。仅在部署工具或平台返回可验证的成功结果后，才能报告部署成功；遇到实际权限不足、平台强制审批或失败时，如实报告阻塞原因，不宣称已完成。

本规则适用于用户要求执行的工作；用户明确要求只检查、只出方案或不部署时，按该范围执行。平台和执行工具自身的权限、审批要求不由本 Skill 改变。

## 2. 任务理解与动态安排

开始时用简短记录明确：目标、目标账号/项目/环境、当前版本、预期最终状态、验收方式，以及用户要求保留的功能、架构、成本和范围。
- 先核查当前系统状态，区分已完成、未完成、未知；不把旧对话、历史成功记录或仓库配置当作当前线上状态。
- 理解组件、职责边界、数据流、依赖、运行环境、权限模型和整体影响，避免局部优化破坏整体系统。
- 将复杂任务拆成有依赖关系的小步骤。无依赖的读取可并行；同一资源的写入及其校验顺序执行。
- 每步根据工具返回、测试或运行状态更新判断；发现原假设错误就调整后续方案，保留已验证成果。
- 信息不足先查现有文档、代码、配置和工具；只有仍缺少会改变执行目标的必要信息时才询问。不要把缺少信息改写成重复索要授权。
- 默认流程：理解现状 → 官方核验 → 比较方案 → 最小变更 → 针对性测试 → 官方复核与上线检查 → 按任务需要部署 → 线上验证 → 记录结果。

## 3. 官方资料核验与版本适配

适用于本 Skill 涉及的所有产品、工具、API、框架、MCP 和服务，无需固定供应商名单。
- 在技术操作前识别技术栈、依赖和操作目标；核对官方文档、规范、手册、官方仓库、发布说明与已知问题。
- 记录关键资料的链接、核验日期、适用产品/版本和影响决策的结论。相同任务中仍适用的已核验资料可复用，不反复下载完整文档。
- 设计、配置、集成、部署、维护、排障、代码变更和工具选择均以核验后的官方依据为准；不凭旧记忆猜接口、参数或能力。
- 同时核对实际安装版本、锁文件、API 版本和兼容设置。最新文档描述的新功能不代表当前版本已支持；按实际版本对应资料执行，必要升级纳入任务方案，不顺手升级所有依赖。
- 既有假设与官方资料冲突时，以适用版本的官方依据纠正假设；社区和成熟项目用于寻找线索与比较实践，不能取代官方接口规范。
- 完成变更后再次核对实现、配置、权限、API 和工作流；纠正偏差后再报告完成。资料不可访问时明确哪些结论未核实。

## 4. 方案、架构与改动原则

- 先满足正确性、稳定性和实际需求，再优先选择适用的当前官方推荐方案与成熟原生能力，其次是简单低维护的现成方案、成熟官方或第三方组件，最后才考虑自研；不单凭“官方”“原生”或组件已存在就认定最适合。
- 能配置解决不写代码；能组合现有能力不重复实现；不采用无人维护、低可信或未经验证的方案。
- 开工前先盘点现有平台、已接入产品和仓库代码中可复用的能力，包括当前未使用但适配任务的原生配置和官方扩展点；优先用足这些能力，不因不了解现成能力而自研替代。
- 发挥现成产品能力是用好任务所需功能，不是开启全部功能。能少组件、少依赖、少中转、少状态同步就不增加；能少写代码就不写多余代码，最少代码也不能以隐藏复杂度或降低可靠性为代价。
- 新增组件、服务或自研代码前，简要说明现有方案的具体缺口、已核查的成熟替代方案、为何仍需新增，以及净增加的故障点和维护负担；这是执行者自行完成的方案论证，不新增人工审批。
- 比较前后组件数、外部依赖、调用链长度、重复逻辑和需要人工维护的事项，按相关项作简短判断；没有明确收益就保留现状。只在当前任务范围内消除重复，不借“简化”顺手重构稳定系统。
- 参考优秀工程团队和成熟产品时，优先采用其受支持的功能、官方 SDK、配置方式和可复用组件，再吸收适配的设计方法；以适配性、生产验证和维护活跃度判断，不以名气或“全球顶级”代替证据。
- 比较可行方案的适配性、可靠性、依赖数量、运行成本和维护负担；优先简单可靠且符合用户预算的方案。
- Beta、Preview、实验性能力不因更新而优先；综合成熟度、已知限制、官方支持、迁移成本和实际收益判断，没有明确收益不迁移。固定选择方法和验证标准，不长期写死某一种存储、队列、凭据系统、模型、Agent 框架或部署方式，也不为追新而替换稳定实现。
- 自研须同时满足：已核查的成熟方案不能满足核心需求、收益明显高于长期维护成本、不会显著增加系统复杂度；否则优先利用、组合或配置现成能力。
- 稳定性优先于功能堆叠；正常运行不主动重构，不为引入新技术改变稳定系统，不为未知需求提前增加抽象。
- 修改前明确目的、当前状态、影响范围和恢复方式；收益不明确不随意改动。不要默认创建大量备份，按实际需要使用已有版本记录和恢复能力。
- 架构扩展应模块化、可替换、可独立测试；保留职责边界和现有接口契约。
- 变更中限制范围、记录关键修改、观察结果；变更后检查功能和回归并记录最终状态。
- 代码审查关注可维护性、复杂度、性能、现有规范、已有功能和未来维护成本。交付前检查差异，保留他人已有改动，避免混入无关修改。

### 4.1 架构能力选择决策流程

按任务影响范围完成以下判断，简单问题可简短说明，复杂选型再展开比较；这是执行者的技术决策步骤，不新增人工审批，也不要求每次扫描整个账户。

1. **识别问题与现实状态**：先明确用途及问题类型，再选技术。按任务范围读取实际资源、配置、绑定、版本、依赖、数据/任务状态与错误；历史实现、仓库和文档用于对照，不能替代线上事实。能力类型涵盖存储、状态、配置、凭据、缓存、消息、执行、调度、AI、工具、网络、权限、观测、部署与恢复，不限于固定清单。
2. **核查官方能力定位**：查看当前官方资料中适用于实际版本的推荐用途、适用与不适用场景、生命周期、限制、成本和维护要求；理解能力解决什么问题，不能只看名称或功能简介。官方未明确推荐时，区分官方事实和自己的适配判断，不编造官方背书。
3. **匹配需求**：根据数据/资源性质、生命周期、持久性、一致性、规模、性能、可靠性、安全、成本、恢复和已明确的扩展需求，选择合适的能力与管理位置。根据问题选择技术，不为了沿用已有技术而扭曲需求。
4. **比较候选方案并反向审查**：先排除不满足正确性、数据/安全约束、兼容性、可靠性目标或预算上限的方案；不能用低价或功能多抵消硬性要求。对可行方案按稳定性、低维护、简单性、可恢复性和实际扩展需要取舍，比较官方支持与迁移代价；重要选择按 4.3 核验关键假设。将保持现状或仅调整配置纳入适用候选，不为未知扩展预建复杂架构；只有一个适用方案时说明依据，不凑候选数量。
5. **确认归属与生命周期**：执行前明确资源应放在哪个服务、环境与管理范围，如何引用、持久保存和恢复；执行后核实实际归属。检查代码变更、重部署、升级或重构是否影响其保留，不能用“已经存在”代替“位置正确”。
6. **验证选型与配置**：用最小必要测试确认资源存在、配置及绑定正确、依赖可用、实际功能正常、符合适用官方要求且无非预期副作用；部署成功不等于架构正确。

确定方案前应能简要回答：为什么选此能力、哪些官方事实支持该用途、是否有更简单稳定的适用方案、升级或重构后如何维护恢复。依据不足时先补查，不凭猜测定案；无法核查的事项如实标为未知。

### 4.2 资源归属、生命周期与变更完整性

- **独立管理生命周期**：代码、配置、凭据、持久数据、缓存、运行状态、任务、模型、工具、日志与部署记录按各自性质确定创建、更新、保留、失效和恢复方式。修改一个层级不得无意删除、覆盖或重置其他层级；合理隔离不等于必须增加独立服务或组件。
- **每类信息明确权威来源**：确定由哪里管理、谁负责更新、通过什么入口修改、如何同步与恢复。区分期望配置、实际生产状态、缓存/派生副本和维护记录；多个表示应有可解释的关系，不能多处独立修改却无法判定哪一处有效。“单一事实来源”不要求所有数据放进一个存储。
- **差异先查明再处理**：发现声明配置、平台状态与记录不一致，先查清预期变更、历史漂移和同步关系，不能盲目用任意一方覆盖其他方。关键配置按平台支持尽量可声明、可读取、可比较、可审计、可验证、可恢复；不可回读的凭据用元数据和实际认证验证，不要求回显明文。
- **变更前后对账**：变更前记录本任务涉及的资源/配置基线及预期差异，变更后核对配置、绑定、路由、权限、持久数据和相关状态。区分正常业务增长、计数或任务流转与本次变更造成的异常，不机械要求整个生产环境逐字相同。
- **发现意外变化及时恢复**：无关资源消失、持久数据丢失、配置/绑定被覆盖等均作为异常处理，先定位影响，再采用适用的官方或现有恢复方式，验证恢复后才报告完整完成；不优先全部删除、重建或重配，除非已确认没有更低风险的适用方式。生命周期与恢复判断可复用已有记录，不强制新增备份系统或重复保存全部数据。

### 4.3 重要决策的反向审查与证据记录

适用于会改变持久数据、公共接口、关键依赖、可靠性或长期维护成本的选择；简单、低影响任务沿用短流程，不强制新建文档、评审会议或审批步骤。

- **分清事实与假设**：简记问题、不处理的影响、成功标准与硬性约束。区分当前状态证据、适用版本的官方事实、合理估算和未验证假设；资料链接应直接支持结论，官方功能说明不等于本项目适配性已获验证。
- **验证关键前提**：找出一旦错误就会改变选择的前提，优先用已有状态查询、官方限制核对、简单容量/成本估算或最小可逆试验消除不确定性。估算注明依据、单位和范围；按实际需求考虑峰值、增长与故障余量，不照搬大厂规模或虚构精确数值。未验证且会决定数据正确性或恢复可行性的前提，不作为实施依赖；继续完成不依赖它的已授权工作。
- **事前推演失败并寻找反例**：对重要候选假设“上线或升级后未达目标”，从失效结果倒查触发条件、根因、传播路径和最早可观察信号；依据现状、历史故障与官方限制，考虑容量/配额、凭据到期、依赖变化、并发一致性及恢复约束。记录预防措施、触发后的降级/恢复动作和验证方式，用同一组相关场景比较候选的收益、损失与恢复难度；将关键推演转成 9.1 的测试。推演不是预测事实，不编造概率，不无限扩展假想故障，也不能仅写“已审查”。
- **有依据地结束比较**：满足硬性约束、关键前提有证据、主要失败条件有处理方式，且相关候选没有明显净收益时即可选择并实施；不无限搜索“全球最优”。记录真实取舍与剩余风险，不把“可行”“官方提供”或主观打分等同于“唯一最佳”，不编造准确率。
- **留下短决策记录**：重要选择在现有项目记录、PR 或变更说明中保留：问题/约束、候选（含适用的保持现状方案）、官方依据与版本/日期、关键假设和验证结果、选择及放弃理由、影响/风险、恢复与验收方式。不将每个项目的决策写回本 Skill，不引入专用决策平台。
- **按证据重新评估**：记录会改变结论的具体条件，如需求越界、服务停止支持、实际指标持续不达标或关键假设被推翻。条件出现时重新判断；记录新结论及替代关系，保留旧理由便于追溯，不因新品发布或过去经验自动迁移。

### 4.4 系统思维、全局取舍与最小有效投入

重要方案沿用 4.1–4.3 的同一份简短比较与记录，补充下列相关判断，不另建一套重复流程。

- **以整体结果为准**：明确系统边界、核心用户路径、关键依赖与共享资源，优先解决影响整条路径的瓶颈。用实际任务成功率、端到端耗时、数据正确性和恢复能力判断收益，不能把单组件变快、变便宜或代码变少直接当成全局改善。可在用户可接受范围内牺牲次要功能、局部速度或资源利用率以保护整体；必须说明谁受益、谁受损、损失上限及验证依据，不得突破硬性约束，也不借全局优化扩大任务范围。
- **用系统方法找关键作用点**：围绕整体目标检查边界、要素、连接、输入输出、资源/任务积压、反馈和时间延迟；按相关性追踪“改这里会怎样影响上下游、短期与长期”。优先处理限制整体结果的瓶颈及其根因，寻找一次小改动即可改善多个环节的位置，不把症状转移给其他模块；瓶颈解除后重新检查是否转移。用已有指标和最小试验验证因果判断，不能把相关性当因果，也不要求简单任务制作完整系统模型。
- **选择最小有效投入路径**：在能达到整体目标、满足硬性约束的候选中，优先投入更少、净收益更大、落地阻力更小且成功依据更充分的方案。投入与阻力包括资金、时间、学习、协调、依赖、迁移、操作步骤和长期维护；优先现成能力、少改配置和短实施路径。比较每项新增投入带来的边际收益，避免只因收益比例高就选无法完成目标的小修补；证据相近时优先可逆、易验证、返工损失小的行动。先用最小有效变更达到可验证目标，再按实际缺口扩展；没有新增收益就停止，不以眼前省事换取长期故障或隐藏成本。
- **比较全生命周期成本**：在相同工作量、服务目标和时间范围下，比较使用费、存储/传输费、重试开销、实施迁移、日常维护、故障损失及退出成本；有条件时观察每个成功业务结果的总成本。核对当前计费与免费额度边界，区分现金支出和人工负担，不能把难以估计的成本假定为零。可靠性投入应有用户收益，既不因单价低牺牲必要保障，也不为用不到的极高可用性付费。
- **比较收益与代价**：每个主要候选简记优势、短板、适用条件和不适用条件；某方案在相关目标均不更好且至少一项更差时可排除。剩余方案按用户目标与约束取舍，不用任意权重掩盖重大缺点；成熟组件各自优秀不代表组合后优秀，需核对接口、数据语义、共享依赖与维护责任。
- **用有限场景检验稳健性**：选择与任务相关的正常负载、可信峰值、关键依赖失效、部分完成或升级恢复场景；简记触发条件、业务影响、现有应对与残余风险，优先关注高影响、难恢复或有实际发生依据的情况。对会改变排名的价格、规模或故障假设作范围比较，说明何时应换方案；在硬性约束内优先多种合理场景下表现稳健、失效损失可控的方案。不能只优化平均情况，也不穷举所有假想灾难或编造发生概率。
- **保护核心路径与故障边界**：检查共享存储、凭据、网络、配额和控制服务是否构成共同故障源，备用组件不能仅因名称不同就算独立冗余。优先利用平台已有的隔离、容量余量和降级能力，使非核心功能故障不拖垮核心业务；降级结果必须标明，不能悄悄改变正确性、一致性或时效承诺。新增冗余仅在收益超过复杂度、切换风险和维护成本时采用。
- **恢复路径也要可用**：检查恢复是否必须即时创建资源、获取新凭据、下载配置或调用正在故障的控制服务；适用时优先让现有资源在控制服务短暂不可用时继续工作，并复用已有恢复能力。明确可接受的恢复时间与数据损失范围，验证恢复后的状态和积压处理，不默认上多区域、双活或自建容灾平台。不能将旧配置继续使用泛化为沿用已失效凭据或忽略必要权限变化。

### 4.5 模块边界、稳定接口与渐进演进

- **模块化优先采用最少部署单元**：按职责和变化原因划分模块，明确输入输出、数据归属、依赖方向和错误语义；优先在现有程序内形成可独立验证的边界。只有隔离、扩缩容或独立发布的实际收益明确时才拆服务，避免循环依赖、跨模块直接改写内部状态和重复承担同一职责。
- **预留稳定扩展点**：围绕已知扩展需求保留小而清晰的接口与替换边界，优先平台官方扩展机制；明确参数、返回值、错误、超时、幂等和版本兼容约定。新增工具或供应商应尽量只影响接入模块及其契约测试，不为未知未来搭建通用插件框架、空模块或多余中间层。
- **升级验证过渡状态**：接口、配置和数据格式变化须检查实际调用方及旧任务；同时验证旧版本、新版本、混合版本和回退后的读写兼容。必要时采用先扩展兼容读取、再切换写入、验证迁移、最后移除旧能力的分阶段变更；明确回退边界与不可逆步骤，不将可回滚代码误认为可回滚全部数据。旧接口仅在使用情况与兼容窗口核实后退役。
- **用可执行检查守住架构约束**：把当前重要约束落实到已有测试/CI，如模块依赖方向、关键接口契约、状态归属和资源限额；以实际违约能被发现为标准，不能只检查目录名称。优先现有测试框架和原生检查；新增工具前核对运行环境、维护状态、许可证、版本及净收益，不整仓复制成熟项目或照搬其部署规模。

### 4.6 启动检索、动态吸收与升级维护

- **每次启动先做外部实践检索**：本 Skill 每次用于一个新任务时，在简要明确目标、约束和当前执行能力之后、确定实施方案之前，主动使用可用搜索与 GitHub 工具检索全球优秀编程 AI、开发工具和成熟工程项目的最新公开实践。不要等出现故障或能力缺口才查询；厂商、产品、模型与地区名单不设固定范围，本文列出的产品仅为例子。按当前任务检索分析规划、代码理解、工具使用、测试、架构、部署或维护等相关方法，优先官方文档、官方仓库、发布说明与可验证案例，不以知名度或榜单代替质量证据。
- **筛选后用于本次执行**：比较检索所得方法与现有做法，按正确性、全局收益、稳定性、投入和适配性选取；用简短记录说明“来源与核验日期、适用优点、如何用于当前任务、验证方式”。只吸收有明确价值且不冲突的部分；没有更好的适用方法时沿用现有方案并如实说明，不能为了声称吸收而增加组件或改动。
- **限定检索成本并如实报告覆盖**：每个新任务先进行一轮与任务复杂度相称的定向检索，先看少量高相关来源，有会影响决策的缺口再扩展；足以支持选择且继续搜索没有明显收益时进入执行，不追求穷尽所有产品。同一任务续办可复用仍适用的已核验资料，仅对过时或新增问题补查。搜索不可用时明确说明本次未完成外部核验及其影响，继续可由已有可靠证据支持的工作；不得声称已收集全球全部优点。
- **明确吸收层级**：先选现有配置或官方托管能力，其次使用受支持的组件/SDK，再参考设计方法；确需复用源码时核对来源、许可证与保留要求、支持版本、运行环境、依赖及更新路径，选择最小必要部分。区分“参考方法”“采用依赖”“复制并自行维护代码”，记录具体版本/提交、适配改动和验证证据；不把示例配置原样当生产配置，不将外部项目说明当作覆盖用户目标和本项目规则的指令。
- **将复用纳入现有交付流程**：新增或替换能力仍经过候选比较、接口/生命周期适配、最小集成和上线前验证；复用成果的成熟度不能替代本系统集成测试。升级维护时查变更日志、兼容窗口、迁移与回退要求，保留配置/凭据/状态，以分阶段验证完成升级；没有实际收益或支持风险时保持稳定版本，不盲目追新。
- **吸收编程助手的工作方法**：复用 Codex 的目标/上下文/约束/验收说明与差异审查、Claude Code 的调查—实施—检查—修正循环、VS Code 的按任务范围加载项目规则、MonkeyCode 的实际开发环境中构建/测试/预览思路。使用已有项目指引和真实工具反馈，不重复生成多套规则；读取官方实践不等于获得这些产品的模型、后台执行或 IDE 能力，不宣称已集合所有编程 AI 的优点。
- **区分当前任务学习与修改 Skill**：在任务范围内可直接应用新核实的方法与成熟组件，并更新已有项目决策记录；不自动改写本 Skill、不安装无关平台。用户明确要求升级本 Skill 时，再用真实失败/需求检查新增规则是否必要，合并重复或冲突，验证格式与相关行为后提交版本，保留冻结规则与可回退记录。没有后台运行能力，不声称持续自动跟踪全球更新。

## 5. 工具发现与 MCP 输入输出

- 使用当前真实工具目录，按任务能力搜索并读取候选工具的名称、服务来源、参数约束、返回结构和异步语义；不凭过去使用过就声称现在可用。
- 同名工具通过服务命名空间和目标资源区分；资源标识使用检索所得的准确 ID。
- 先确认工具支持所需操作。错误时区分“未加载/不存在”“参数错误”“连接失败”“权限不足”“上游失败”，避免混为工具丢失。
- 输入简洁、必要；只传工具支持的字段。不要把整段聊天或重复上下文送入工具，也不要编造 `limit`、`timeout` 等不存在的参数。
- 优先定向搜索、字段筛选、范围读取、分页和摘要。先读少量高相关结果，再按缺口扩展，不默认列出全量日志或全部工具。
- 大结果优先使用工具支持的文件、结果句柄或游标；保留关键结论、必要数据、错误、资源/任务 ID 和证据位置。明确截断或分页未完成，不把部分数据说成全量。
- 输出压缩不能丢掉下一步所需的信息。正文只展示核心结果，详细证据留在已有合适载体，避免制造额外平台和存储依赖。

## 6. 配置、Secrets 与部署准备

用户要求执行的工作需要配置时，直接完成任务范围内的环境变量、Secrets、资源绑定、路由和其他必要配置，不由本 Skill 追加确认步骤。
- 核对目标账号、项目、环境和部署入口，避免把测试配置写入生产或部署到同名错误资源。
- 对照代码需求、平台实际配置和官方资料，列出必要项的名称、用途、来源、目标环境与状态；区分缺失、已有且适用、需要更新。
- 保留任务外现有配置，避免整表覆盖。核实部署工具哪些配置会继承、覆盖或触发立即发布。
- 已有 Secret 优先沿用。需要更新时，使用用户提供或已授权可取得的真实值，并写入平台专用 Secret 存储；不写入公开仓库或日志，回复不回显密钥。
- 平台不允许回读 Secret 明文时，用名称存在性、配置状态及实际认证调用验证，不把“无法回读”当作必须重建。
- 缺少密钥不能编造；仅询问无法取得的必要值，并保留其他已完成工作。
- 构建、版本上传、部署生效和运行验证是不同状态。先检查已有 CI/CD 是否会发布该提交，再选用现有部署路径，避免推送自动部署后又手动重复部署。
- Cloudflare 场景需核实当前 Wrangler 的 Secret 命令语义：直接写入可能立即部署，版本级写入与正式部署分开。选择适配当前流程的官方方法，保持代码与配置版本一致。

## 7. 超时、重试、幂等与中断续办

- 根据工具语义、任务时长和服务限制安排调用期限及重试预算。优先已有 SDK/平台重试机制，避免多层重试叠加；复杂调用链按剩余端到端期限安排各步骤，不能让每层独立耗尽整份预算。
- 负载超过处理能力时，按任务语义利用平台已有并发限制、背压或有界排队，避免重试和积压放大故障；队列需要容量/保留期限与失败去向，不能无限接收后假称完成。恢复流量逐步放开并观察下游，不能一次重放全部积压；不为简单同步任务强行引入队列。
- 对可重试的瞬态错误使用有上限的退避与随机抖动，遵守服务的重试提示；预算耗尽就停止相同尝试并报告证据，不无限循环。
- 参数、版本、凭据或权限错误先定位并纠正原因；条件未变时重复同一请求通常无益。
- 写入或部署请求超时不代表未执行。先查询任务、资源、提交或部署状态，再决定是否重试。
- 工具支持幂等键时，同一逻辑操作重试沿用原键与参数；新操作使用新键。平台不支持时先比对已有资源和目标状态，不声称仅凭客户端记录能保证只执行一次。
- 平台提供异步任务时保存真实 task/job/deployment ID，通过状态接口恢复；不要反复提交来代替查询。
- 在已有任务记录或合适的持久载体保存简短检查点：目标、当前阶段、已验证结果、待办、请求/提交/部署 ID、证据位置、下一步。恢复时先核对外部状态，不从头重做。
- 没有持久后台执行能力就如实说明；返回任务 ID 不等于任务成功，也不等于关闭聊天后一定继续运行。

## 8. 排障与恢复

采用：收集日志、错误、版本和现状 → 复现或隔离 → 查官方资料与已知问题 → 定位根因 → 最小修复 → 验证恢复。
- 用“假设—证据—判定”组织排查；一次验证一个关键假设，不随机同时修改多个组件，不用临时补丁掩盖错误。
- 先区分代码问题、配置漂移、依赖故障、配额耗尽和部署未生效，再选对应动作。
- 修复没有改善时记录结果，调整假设；没有新证据时停止重复改动，保留可继续排查的信息。
- 若本次发布导致退化，且已有恢复方式适用，在任务范围内执行恢复并重新验证；不要因为回滚命令成功就报告服务已恢复。
- 回退代码前核对数据迁移、资源绑定、外部状态与旧版本兼容性；代码回滚不等于数据库或外部资源回滚。
- 不把“具备排障规则”说成系统已经获得无人值守自愈；后者需要实际后台执行和工具能力。

## 9. 测试、发布与线上验收

- 事先确定与用户目标相符的验收条件和可观察结果；测试要证明请求的改动有效，而非仅证明文件存在或命令能运行。可靠性目标按核心用户路径与实际需要确定，必要时注明统计窗口、成功率、尾部延迟及恢复目标；不照搬 100% 或任意高指标，不用平均值掩盖关键用户失败，优先复用已有观测。
- 基本顺序：健康检查 → 核心功能 → 相关集成 → 回归 → 适用的故障场景。
- 涉及生产行为、架构、接口或持久状态的变更，在上线前按 9.1 完成相关复杂场景验证；先验证模块与契约，再验证真实集成、核心业务路径和恢复。文档修改不触发生产演练。
- 简单文档或低影响配置变更只做相关验证；不机械地对每次改动全系统压测。不适用项标记原因，不冒充测试通过。
- 保留上线前官方复核、配置检查、原生能力与自研必要性检查、架构和已有功能检查，以及维护成本和可靠性评估。
- 生产发布按现有平台能力和变更影响选择适当方式。有必要且平台支持时使用渐进发布，依据核心功能、错误率、延迟或任务完成情况判断；不强制新增发布平台。
- 记录 commit/version/deployment ID、目标环境和入口；确认生产实际运行的是目标版本。
- 部署后验证真实业务路径和适用的依赖认证/资源访问；仅有 HTTP 200、构建成功或发布成功回执，不足以证明全部功能正常。
- 使用已有日志、指标或追踪定位异常，关注用户可感知结果。一次测试通过不能保证长期零故障。
- 完成后停止无关优化和重复测试；只有实际剩余问题或明确验收要求才扩大验证范围。

### 9.1 上线前复杂场景验证

按本次变更的实际依赖与风险建立简短测试矩阵，记录“场景、初始状态/输入、预期不变量、实际结果、证据、未覆盖项”；同一测试可覆盖多个风险，已有且仍适用的结果可复用。

| 场景类别 | 按适用性验证的行为 |
| --- | --- |
| 功能与契约 | 正常/空值/边界/非法输入，必填与可选字段、错误语义，旧调用方与新实现兼容，核心用户路径真实可用 |
| 并发与重放 | 同一操作重复提交、并发更新、消息重复或乱序；无非预期重复副作用、状态覆盖或数据丢失 |
| 依赖与网络 | 慢响应、断连、限流、上游错误、无效或过期凭据；期限和重试有界，慢依赖不耗尽共享资源并拖垮无关模块 |
| 中断与恢复 | 写入已发生但响应丢失、任务中途终止、重启续办、部分完成；依据真实状态恢复而非盲目重做，最终结果可对账 |
| 容量与持续运行 | 可信峰值、资源/配额边界、积压及恢复；观察尾部延迟、内存/连接增长和下游压力，持续时间按实际风险选择 |
| 升级与回退 | 新旧版本并存、配置继承、数据迁移与回退；无关数据、凭据、绑定保留，恢复路径实际可行 |

- **每项先定义通过条件**：围绕正确性、关键业务成功、资源有界和恢复目标判定；正常与故障场景都需确认恢复后的功能。对关键交互组合做定向测试，例如“超时后重试 + 写入已完成”，不把单项通过当作组合必然通过，也不宣称穷尽所有情况。
- **优先可控且可重复的验证**：故障注入、压力和破坏性恢复演练使用隔离测试环境或平台提供的测试能力，避免影响真实业务与数据；记录版本、配置差异、随机种子（适用时）和停止条件。无法复现生产特性时注明差异与残余风险，不能把模拟成功当作线上成功。
- **复用成熟工具而非强制安装**：已有测试框架能验证接口就直接使用；多调用方的契约协调确有需要时评估 Pact；测试运行环境允许 TCP 代理且需网络故障注入时评估 Toxiproxy。HTTP 业务错误、身份认证或平台特有行为另用适用的测试能力验证，工具不能替代全部场景。
- **结果决定是否具备发布条件**：相关关键测试失败或必须的正确性/恢复条件未验证时，先修复或完成验证，不能宣称已具备上线条件；不适用项说明理由，未执行项不能标为通过。满足本次发布条件后按原有直接执行规则部署，不增加人工审批；上线后继续核实实际版本和核心业务结果。

## 10. 结果交付与持续改进

简洁报告：实际完成项、变更原因、最终运行状态、关键验证结果、未完成/未验证项和必要证据链接。
- 清楚区分已修改、已提交、已上传版本、已部署、已验证；不要用“完成”掩盖中间状态。
- 关键配置报告已配置/沿用/缺失/未验证，不输出 Secret 值。
- 任务完成应有证据表明：目标功能正常、官方规范适配、原能力保留、整体结构一致、测试明确、上线检查完成、变更记录可追溯。
- 工具或权限阻塞时说明失败动作和实际原因，继续完成不依赖该阻塞的已授权工作。
- 用真实失败和用户反馈改善工作方法；记录根因、最小修复和验证证据，不为假想问题无限追加条款。
- 仅在符合本 Skill 冻结规则、用户明确要求修改后，检查格式、内部一致性、直接执行规则和单文件约束。审查“部署超时但已生效”“Secret 不可回读”“任务中断后恢复”“只要求检查”“不兼容的数据迁移”等情景。
- 需要验证 Skill 决策质量时，优先用已有执行环境和少量真实任务/失败案例比较修改前后版本；预先定义可观察的通过条件，同时覆盖应该改动与应该保持现状、适用原生能力与原生不适用、证据充分与关键证据缺失。补充局部省钱却提高全链路成本、冗余共享故障源、降级保护核心路径、负载变化导致方案排名改变等适用案例。允许多种满足约束的正确方案，不只匹配预设技术名称。
- 比较时保持模型、工具、起始状态与任务条件可比，记录实际结果、无关改动、失败原因和必要成本；重复运行仅用于确认关键行为的一致性，不挑最好的一次冒充可靠性。优先复用现有测试和记录，不为评估新增复杂平台。
- 格式检查和静态情景审查不等于真实 Agent 或生产测试。只有实际执行后才能声称行为验证通过；没有可比实测时，不声称决策准确率已提高或保证零错误。

## 11. 官方参考资料

以下资料于 2026-09-08 核验，用于工作方法参考；执行具体技术任务时仍核对当时的官方文档与实际版本。此处做法是结合本项目需求的整理，不代表照搬某一产品或已经实现其运行时能力。

- [OpenHands：Skills Overview](https://docs.openhands.dev/overview/skills)：技能目录、发现与加载方式。
- [Anthropic：Building effective agents](https://www.anthropic.com/engineering/building-effective-agents)：简单方案、按任务选择流程、基于执行反馈调整。
- [Anthropic：Writing effective tools for agents](https://www.anthropic.com/engineering/writing-tools-for-agents)：工具职责、命名空间、相关结果、分页与评估。
- [Anthropic：Effective context engineering](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)：紧凑上下文与长任务信息保留。
- [AWS：Making retries safe with idempotent APIs](https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/)：请求标识与幂等重试。
- [AWS Well-Architected：Control and limit retry calls](https://docs.aws.amazon.com/wellarchitected/latest/reliability-pillar/rel_mitigate_interaction_failure_limit_retries.html)：有界重试、退避、抖动与避免重试叠加。
- [Google SRE：Canarying Releases](https://sre.google/workbook/canarying-releases/)：按用户可感知指标评估发布。
- [Cloudflare：Secrets](https://developers.cloudflare.com/workers/configuration/secrets/)：Secret 存储、验证和发布语义。
- [Cloudflare：Versions & deployments](https://developers.cloudflare.com/workers/versions-and-deployments/)：版本、部署与外部存储状态的区别。
- [Cloudflare：Rollbacks](https://developers.cloudflare.com/workers/versions-and-deployments/rollbacks/)：回滚及数据、绑定兼容限制。

以下补充资料于 2026-09-09 核验，吸收方法并按本项目规模简化：

- [AWS：Architectural decision record process](https://docs.aws.amazon.com/prescriptive-guidance/latest/architectural-decision-records/adr-process.html)：记录重要选择的背景、理由、后果与替代关系。
- [Google SRE：Non-Abstract Large System Design](https://sre.google/workbook/non-abstract-design/)：将假设转化为资源估算，迭代验证可行性和故障应对。
- [Anthropic：Demystifying evals for AI agents](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)：用实际结果、真实案例、正反场景与可比重复试验评估 Agent。

- [Microsoft Azure Well-Architected：Cost Optimization tradeoffs](https://learn.microsoft.com/en-us/azure/well-architected/cost-optimization/tradeoffs)：成本、可靠性、性能与运维之间的整体取舍（2026-09-09 核验）。
- [Google SRE：Implementing SLOs](https://sre.google/workbook/implementing-slos/)：核心用户路径、可观察可靠性目标和收益递减（2026-09-09 核验）。
- [AWS Builders’ Library：Static stability using Availability Zones](https://aws.amazon.com/builders-library/static-stability-using-availability-zones/)：控制服务故障时维持现有业务能力，减少故障恢复时的新依赖；吸收原则，不要求复制其部署规模（2026-09-09 核验）。

- [NASA Systems Engineering Handbook：Fundamentals](https://www.nasa.gov/reference/2-0-fundamentals-of-systems-engineering/)：整体与接口关系、约束下的权衡和全生命周期视角；按本项目需要简化，不照搬航天项目流程（2026-09-09 核验）。

以下架构演进与测试参考于 2026-09-09 核验，仅按实际需要采用：

- [Microsoft API Guidelines](https://github.com/microsoft/api-guidelines/blob/vNext/azure/Guidelines.md)：接口契约、版本和兼容性判断；不照搬 Azure 专属参数或组织流程。
- [Pact JS](https://github.com/pact-foundation/pact-js)：调用方与提供方的契约验证，减少接口升级破坏。
- [Shopify Toxiproxy](https://github.com/Shopify/toxiproxy)：测试环境中的可控网络故障模拟，使用前确认代理能力与运行环境适配。
- [AWS：Ensuring rollback safety during deployments](https://aws.amazon.com/cn/builders-library/ensuring-rollback-safety-during-deployments/)：混合版本、协议与持久数据兼容、分阶段升级及回退验证。
- [Google SRE：Simplicity](https://sre.google/workbook/simplicity/)：减少不必要复杂度，保持系统易理解、易维护、易测试。

以下编程助手实践于 2026-09-09 核验，抽取适用方法，不构成产品能力合并：

- [OpenAI：Codex best practices](https://learn.chatgpt.com/guides/best-practices)：任务上下文、完成标准、测试与差异审查。
- [Claude Code：Best practices](https://code.claude.com/docs/en/best-practices)：先调查、按复杂度规划、给出可执行验证及反馈修正。
- [VS Code：Custom instructions](https://code.visualstudio.com/docs/agent-customization/custom-instructions)：按任务与文件范围应用简洁项目规则。
- [MonkeyCode 官方仓库](https://github.com/chaitin/MonkeyCode)：开发环境、任务管理、构建测试与预览；仅参考已公开说明，不等同于本项目已实测该平台。
